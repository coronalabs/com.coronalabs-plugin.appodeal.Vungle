Sources for the plugin `plugin.appodeal.Vungle`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.Vungle" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
